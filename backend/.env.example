# Finance Tracker Backend - Environment Variables Example
# 
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#   # Then edit .env with your actual credentials
#
# IMPORTANT: Never commit the .env file to version control!
# The .env file should be in .gitignore

# ==============================================================================
# REQUIRED ENVIRONMENT VARIABLES (for production)
# ==============================================================================

# Spring Profile
# Set to 'prod' for production, 'dev' for development (default)
SPRING_PROFILES_ACTIVE=prod

# MongoDB Configuration
# REQUIRED: MongoDB connection string (MongoDB Atlas for production)
# Example: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&ssl=true
MONGODB_URI=your-mongodb-connection-string-here

# MongoDB Database Name
MONGODB_DATABASE=finance_tracker

# Firebase Configuration
# REQUIRED: Base64 encoded Firebase service account JSON
# To encode: cat firebase-service-account.json | base64
# Alternative: Use GOOGLE_APPLICATION_CREDENTIALS environment variable pointing to file path
FIREBASE_CREDENTIALS_BASE64=your-base64-encoded-firebase-credentials-here

# Google OAuth Configuration
# REQUIRED: Google OAuth client ID (Web client ID)
WEB_CLIENT_ID=your-google-oauth-client-id-here
